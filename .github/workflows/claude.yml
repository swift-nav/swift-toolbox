name: Claude

on:
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]
  pull_request_review:
    types: [submitted]
  pull_request:
    types: [review_requested, ready_for_review]
  issues:
    types: [opened]

jobs:
  claude_review:
    uses: swift-nav/swift-context-hub/.github/workflows/claude.yml@v0
    secrets:
      anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
      SSH_KEY: ${{ secrets.SSH_KEY }}

