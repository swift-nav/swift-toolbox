import "../Constants"
import QtCharts
import QtQuick
import QtQuick.Controls
import QtQuick.Layouts
import SwiftConsole

Item {
    id: advancedInsTab

    width: parent.width
    height: parent.height

    ColumnLayout {
        anchors.centerIn: parent

        FusionStatusFlags {
        }
    }
}
